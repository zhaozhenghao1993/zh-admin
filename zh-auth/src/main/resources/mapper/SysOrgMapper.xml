<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.zhenghao.zh.auth.dao.SysOrgMapper">

	<resultMap id="OrgListResult" type="SysOrgEntity">
		<id property="orgId" column="org_id" />
		<result property="parentId" column="parent_id" />
		<result property="ancestors" column="ancestors" />
		<result property="orgName" column="org_name" />
		<result property="orderNum" column="order_num" />
		<result property="createTime" column="create_time" />
		<collection property="children" javaType="ArrayList" ofType="net.zhenghao.zh.auth.entity.SysOrgEntity" column="org_id"
					select="listParentId" />
	</resultMap>

	<resultMap id="TreeListResult" type="SysTreeVO">
		<id property="key" column="org_id" />
		<result property="parentId" column="parent_id" />
		<result property="value" column="org_id" />
		<result property="title" column="name" />
		<collection property="children" javaType="ArrayList" ofType="net.zhenghao.zh.auth.vo.SysTreeVO" column="org_id"
                    select="listTreeParentId" />
	</resultMap>

	<select id="list" resultMap="OrgListResult">
		SELECT
			o.org_id,
			o.parent_id,
			o.org_name,
			o.order_num,
			o.create_time
		FROM
			sys_org o
		WHERE
			o.parent_id = 0
		ORDER BY
			o.order_num ASC
	</select>

	<select id="listParentId" parameterType="java.lang.Long" resultMap="OrgListResult">
		SELECT
			o.org_id,
			o.parent_id,
			o.org_name,
			o.order_num,
			o.create_time
		FROM
			sys_org o
		WHERE
			o.parent_id = #{parentId}
		ORDER BY
			o.order_num ASC
	</select>

	<select id="listTree" resultMap="TreeListResult" flushCache="true">
		SELECT
			o.org_id,
			o.parent_id,
			o.org_name
		FROM
			sys_org o
		WHERE
			o.parent_id = 0
		ORDER BY
			o.order_num ASC
	</select>

	<select id="listTreeParentId" parameterType="java.lang.Long" resultMap="TreeListResult">
		SELECT
			o.org_id,
			o.parent_id,
			o.org_name
		FROM
			sys_org o
		WHERE
			o.parent_id = #{parentId}
		ORDER BY
			o.order_num ASC
	</select>

	<select id="getChildCountByMenuId" resultType="java.lang.Integer">
		SELECT
			COUNT(1)
		FROM
			sys_org
		WHERE
			parent_id = #{value}
	</select>

	<insert id="save">
		INSERT INTO sys_org (
			parent_id,
			ancestors,
			org_name,
			order_num,
			creator_id,
			create_time
		)
		VALUES (
			#{parentId},
			#{ancestors},
			#{orgName},
			#{orderNum},
			#{creatorId},
			now()
		)
	</insert>

	<select id="getObjectById" resultType="SysMenuEntity">
		SELECT
			org_id,
			parent_id,
			ancestors,
			org_name,
			order_num,
			create_time
		FROM
			sys_org
		WHERE
			org_id = #{value}
	</select>
	
	<update id="update">
		UPDATE sys_org
		<set> 
			<if test="parentId != null">parent_id = #{parentId}, </if>
			<if test="ancestors != null">ancestors = #{ancestors}, </if>
			<if test="orgName != null">org_name = #{orgName}, </if>
			<if test="orderNum != null">order_num = #{orderNum}, </if>
			<if test="modifierId != null">modifier_id = #{modifierId},</if>
			modified_time = NOW()
		</set>
		where org_id = #{orgId}
	</update>

	<delete id="remove">
		DELETE
		FROM
			sys_org
		WHERE
			org_id = #{value}
	</delete>

	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>
	
</mapper>